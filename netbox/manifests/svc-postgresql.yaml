apiVersion: v1
kind: Service
metadata:
  annotations: {}
  name: postgresql
  namespace: netbox
spec:
  clusterIP: None
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: postgresql
