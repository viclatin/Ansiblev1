# Network Operations Agent

Run the agent from the project root with:

```bash
cd /home/victor/ansible
/home/victor/ansible/scripts/venv/bin/python -m agents.network_agent "Run a compliance assessment"
```

For interactive use:

```bash
cd /home/victor/ansible
/home/victor/ansible/scripts/venv/bin/python -m agents.network_agent
```
